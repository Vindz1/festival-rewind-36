name: Maintenir Supabase actif

on:
  schedule:
    - cron: '0 */6 * * *'  # Toutes les 6 heures
  workflow_dispatch:  # Permet de tester manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping le site
        run: |
          echo "Ping de setlive.fr..."
          curl -s https://setlive.fr > /dev/null
          echo "✓ Ping réussi!"
